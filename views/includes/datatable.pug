.card
    .card-body
        .row
            .col
                table#dataTable.table.table-sm.table-hover.mb-0
    .card-footer
        .row.align-items-center.justify-content-center
            form.mb-0#uploadform(method="POST" action="/uploadfile" encType="multipart/form-data")
                .form-row.align-items-center
                    .col-10
                        input.form-control-file(type='file' name='file')
                    .col-2
                        button.btn.btn-primary.float-right(type='submit') Upload